import 'package:flutter/material.dart';

class RefineModel {
  String name;
  bool selected;
  Color selectedColor;
  Color unSelectedColor;

  RefineModel(
      this.name, this.selected, this.selectedColor, this.unSelectedColor);
}
